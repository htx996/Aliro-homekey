# Overlay that turns this firmware into a Matter door lock, on top of
# sdkconfig.defaults. Build with:
#
#   idf.py -D SDKCONFIG_DEFAULTS="sdkconfig.defaults;sdkconfig.matter" build
#
# inside the espressif/esp-matter container (or with ESP_MATTER_PATH exported),
# which is what .github/workflows/matter-firmware.yml does.

CONFIG_ALIRO_MATTER_ENABLE=y

# --- Commissioning ---------------------------------------------------------
# A device that is not on the network yet can only be reached over BLE, so
# BLE is how commissioning starts even though the lock then lives on Wi-Fi.
CONFIG_BT_ENABLED=y
CONFIG_BT_NIMBLE_ENABLED=y
CONFIG_BT_NIMBLE_ENABLE_CONN_REATTEMPT=n

# esp-matter defaults this on: the moment the first fabric commissions, it
# calls esp_bt_mem_release() and hands BLE's RAM to the general heap --
# irreversible for the boot session, by Espressif's own documentation.
# DriveBLEState() does have a path to re-init BLE for a later commissioning
# window, but it cannot succeed once that memory is gone, and the failure
# surfaces as a silent inability to re-advertise rather than a clear error.
# That matches two reported symptoms exactly: a second ecosystem failing to
# commission once one fabric already exists, and removing + re-adding a
# fabric failing until the device is rebooted. See issues #8 and #10. The
# ESP32 has the RAM to keep BLE resident; trading that for reliable
# re-commissioning is worth it here.
CONFIG_USE_BLE_ONLY_FOR_COMMISSIONING=n

# Nothing on this device is a BLE client or scanner. Dropping the roles it
# does not use is ~30 KB.
CONFIG_BT_NIMBLE_ROLE_CENTRAL=n
CONFIG_BT_NIMBLE_ROLE_OBSERVER=n

# --- Wi-Fi -----------------------------------------------------------------
# ENABLE_WIFI_AP is chip's own access point, which this project does not use.
# The IDF SoftAP driver stays on: net_manager raises the setup portal with it,
# and that portal is the only way to configure a board that has never been
# commissioned.
CONFIG_ENABLE_WIFI_AP=n
CONFIG_ESP_WIFI_SOFTAP_SUPPORT=y

# Sockets are a system-wide budget and this build has an unusual number of
# claimants: chip takes several for mDNS, CASE and commissioning, then the
# SoftAP's DHCP server, the captive DNS responder and the configuration web
# server all want their own. At the default of 10 the web server lost the
# race and failed to start with ENOBUFS, leaving a device whose only
# configuration path is that server. 16 is lwIP's ceiling.
CONFIG_LWIP_MAX_SOCKETS=16

# Matter is IPv6 end to end, including on Wi-Fi. One link-local address plus
# one unique local address per fabric.
CONFIG_LWIP_IPV6_AUTOCONFIG=y
CONFIG_LWIP_IPV6_NUM_ADDRESSES=6
CONFIG_LWIP_HOOK_IP6_ROUTE_DEFAULT=y
CONFIG_LWIP_HOOK_ND6_GET_GW_DEFAULT=y

# --- Coming back after a reboot --------------------------------------------
# A controller watches a lock through a subscription, and this device discards
# every non-persisted subscription on power down. That is what leaves Apple
# Home on "Updating..." or "No Response" until it rediscovers the accessory
# and creates a replacement subscription.
#
# CONFIG_ENABLE_PERSIST_SUBSCRIPTIONS is the ESP32 Kconfig symbol that the CHIP
# component maps to the real chip_persist_subscriptions GN argument. Do not use
# CONFIG_CHIP_PERSIST_SUBSCRIPTIONS: it is not a Kconfig symbol, and ESP-IDF
# only warns before ignoring it.
#
# connectedhomeip derives subscription-timeout resumption from the same GN
# argument. That retry is important here because Matter starts before the
# application-controlled Wi-Fi station joins the network; a first CASE attempt
# can therefore run before operational mDNS is ready.
CONFIG_ENABLE_PERSIST_SUBSCRIPTIONS=y

# --- What we do not need from esp-matter -----------------------------------
# This firmware already has an OTA page, a serial console and a web UI, so the
# Matter equivalents are all cost and no benefit.
CONFIG_ENABLE_OTA_REQUESTOR=n
CONFIG_ENABLE_CHIP_SHELL=n

# Our own REPL is off here too, and not to save space: starting it swaps
# stdout onto the driver-based UART VFS, and with chip, NimBLE and Wi-Fi all
# logging concurrently that path asserted inside a blocking semaphore take on
# real hardware, then recursed through the panic handler's own logging. See
# components/serial_console/Kconfig.
CONFIG_ALIRO_CONSOLE_ENABLE=n
CONFIG_CHIP_ENABLE_BDX_LOG_TRANSFER=n
CONFIG_SUPPORT_GROUPCAST_CLUSTER=n

# Two: the root node on endpoint 0, and the door lock. The root node *does*
# count against this -- setting it to 1 leaves no room for the lock itself, and
# is also what made esp_matter_bridge fail to compile with a negative array
# size, since it derives its own limit by subtracting the root endpoint.
CONFIG_ESP_MATTER_MAX_DYNAMIC_ENDPOINT_COUNT=2
CONFIG_ESP_MATTER_NVS_USE_COMPACT_ATTR_STORAGE=y

# --- Crypto ----------------------------------------------------------------
# sdkconfig.defaults turns the long SHA variants off because the Aliro
# transaction is SHA-256 throughout. The Matter SDK's crypto layer is not, so
# they come back for this build.
CONFIG_MBEDTLS_SHA384_C=y
CONFIG_MBEDTLS_SHA512_C=y
CONFIG_MBEDTLS_HKDF_C=y

# --- Fitting in the RAM ----------------------------------------------------
# Trimmed because the web server was failing to allocate a 1 kB request
# scratch buffer on hardware while chip, BLE and Wi-Fi were all resident:
#
#     E httpd_parse: Unable to allocate the scratch buffer
#
# Static RX buffers are 1.6 kB each and permanently resident; the dynamic
# counts are ceilings on what the driver may take from the heap at peak. The
# TCP window is deliberately left alone -- shrinking it would slow the 1.8 MB
# OTA upload, which is the one transfer this device really has to finish.
CONFIG_ESP_WIFI_STATIC_RX_BUFFER_NUM=6
CONFIG_ESP_WIFI_DYNAMIC_RX_BUFFER_NUM=16
CONFIG_ESP_WIFI_DYNAMIC_TX_BUFFER_NUM=16

# --- Fitting in the image --------------------------------------------------
# Measured on the ESP32: without these the link overflows IRAM by ~5.7 KB, and
# the application does not fit a 1.875 MB OTA slot.
CONFIG_COMPILER_OPTIMIZATION_SIZE=y
CONFIG_COMPILER_OPTIMIZATION_ASSERTIONS_SILENT=y
CONFIG_COMPILER_SAVE_RESTORE_LIBCALLS=y
CONFIG_ESP_WIFI_IRAM_OPT=n
CONFIG_ESP_WIFI_RX_IRAM_OPT=n
CONFIG_FREERTOS_PLACE_FUNCTIONS_INTO_FLASH=y
CONFIG_SPI_FLASH_ROM_IMPL=y

# The Matter task and the event task both run deeper stacks than an IDF
# application usually needs.
CONFIG_ESP_SYSTEM_EVENT_TASK_STACK_SIZE=4096
CONFIG_FREERTOS_HZ=1000

# app_main brings the whole Matter stack up on its own stack, on top of
# everything the ordinary build already does there. That build reported 4596
# bytes of 8192 still free; this one has no such margin to report, because it
# panicked before it got that far.
CONFIG_ESP_MAIN_TASK_STACK_SIZE=12288

# Deeper than esp-matter's own examples need, because commands land on this
# task and two of ours are expensive: SetCredential parses a PEM and derives a
# key slot (mbedTLS parse plus a PSA import), and SetAliroReaderConfig converts
# two keys and then rebuilds the reader, which parses them again. This project
# has already been bitten once by that path -- on a short stack it does not
# crash, it fails soft with ESP_FAIL, which reads exactly like a malformed key.
CONFIG_CHIP_TASK_STACK_SIZE=10240
